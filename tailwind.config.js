/**
 * 주요 포인트
 * tailwind intellisense 플러그인 이슈 때문에
 * 각 project의 branch 에서 해당하는 tailwind config 파일 path를 설정해서 사용합니다
 */
const config = require('./packages/todo/tailwind.config.js');
module.exports = config;
