{
  "name": "todo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "next dev",
    "build": "next build",
    "real": "next start",
    "tsc": "tsc",
    "test": "TZ=UTC jest --watch --maxWorkers=2",
    "test_ci": "TZ=UTC jest --maxWorkers=2",
    "lint": "next lint  -d . && stylelint **/*.scss --config .stylelintrc.js",
    "format": "npx prettier --write \"**/*.{ts,tsx}\" --config ../../.prettierrc"
  }
}
