{
  "name": "shared",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "tsc": "tsc",
    "test": "TZ=UTC jest --watchAll --maxWorkers=2",
    "test_ci": "TZ=UTC jest --maxWorkers=2",
    "lint": "eslint --cache . --ext .ts,.tsx,.js && stylelint **/*.scss --config .stylelintrc.js",
    "format": "npx prettier --write \"**/*.{ts,tsx}\" --config ../../.prettierrc"
  }
}
